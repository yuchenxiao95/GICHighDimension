% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GICSelection.R
\name{GICSelection}
\alias{GICSelection}
\title{GIC-Based Variable Selection}
\usage{
GICSelection(
  X,
  Y,
  Initial_Column,
  Calculate_GIC,
  Calculate_GIC_short,
  k,
  Nsim = 1L
)
}
\arguments{
\item{X}{Numeric matrix (n × p) of predictors.}

\item{Y}{Numeric response (vector length n, or matrix with n rows).}

\item{Initial_Column}{Integer vector of initial feature indices.}

\item{Calculate_GIC}{Character: name of the Julia \strong{full} criterion, e.g. "Calculate_BIC".}

\item{Calculate_GIC_short}{Character: name of the Julia \strong{short} criterion, e.g. "Calculate_BIC_short".}

\item{k}{Integer control parameter consumed by the Julia driver (e.g., target sparsity).}

\item{Nsim}{Integer simulations to run (default 1).}
}
\value{
A list with
\itemize{
\item \code{GIC_values}: criterion trajectory (as returned by Julia)
\item \code{selected_coeffs}: indices of selected variables
}
}
\description{
Performs variable selection using the Generalized Information Criterion (GIC)
with Hopfield Network optimization. Heavy computations are delegated to Julia
(via {JuliaCall}); this R wrapper prepares data and function handles.
}
\details{
You may pass any Julia criterion pair by name via \code{Calculate_GIC} and
\code{Calculate_GIC_short}. The candidate-pool size \code{P} is computed internally
in Julia; it is not an R argument. The tuning integer \code{k} (e.g., target
sparsity or related control used by your Julia routine) is passed through.
}
