% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/penalty_selection.R, R/zzz.R
\name{setup_julia}
\alias{setup_julia}
\title{Initialize 'Julia' Environment}
\usage{
setup_julia(
  julia_path = NULL,
  install_julia = FALSE,
  install_deps = TRUE,
  quiet = FALSE
)

setup_julia(
  julia_path = NULL,
  install_julia = FALSE,
  install_deps = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{julia_path}{Optional path to Julia: either the binary (e.g. /path/to/bin/julia)
or JULIA_HOME (a directory containing bin/). If NULL, uses system default.}

\item{install_julia}{Logical; if TRUE, allows JuliaCall to attempt a Julia install (not recommended for CRAN). Default FALSE.}

\item{install_deps}{Logical; if TRUE, check for required Julia packages (no auto install). Default TRUE.}

\item{quiet}{Logical; suppress status messages. Default FALSE.}
}
\value{
Invisibly TRUE on success, otherwise throws an error.

Invisibly TRUE on success, otherwise throws an error.
}
\description{
Sets up the connection to 'Julia' via 'JuliaCall'. Run this before calling functions
that rely on Julia. This function does \strong{not} install Julia by default (CRAN-friendly).

Sets up the connection to 'Julia' via 'JuliaCall'. Run this before calling functions
that rely on Julia. This function does \strong{not} install Julia by default (CRAN-friendly).
}
