% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/BetaEstimate.R
\name{Beta_estimate}
\alias{Beta_estimate}
\title{Coefficient Estimation via Julia}
\usage{
Beta_estimate(
  X,
  Y,
  family = "auto",
  link = "auto",
  n_trials = NULL,
  add_intercept = TRUE
)
}
\arguments{
\item{X}{Numeric design matrix (\eqn{n \times p}).}

\item{Y}{Numeric vector (\eqn{n}) or matrix (\eqn{n \times q}) of
responses.  A one-column matrix is automatically converted to a
vector.}

\item{family}{Character/symbol; default \code{"auto"}.}

\item{link}{Character/symbol/Julia \code{Link}; default \code{"auto"}.}

\item{n_trials}{Integer trials for a binomial response (default
\code{NULL}).}

\item{add_intercept}{Logical.  If \code{TRUE} (default) the wrapper
asks Julia to prepend a column of 1s and \strong{keeps the intercept
coefficient} in the returned vector/matrix.  Set to \code{FALSE}
if \code{X} already contains an intercept column.}
}
\value{
Numeric vector or matrix of coefficients
(intercept first if \code{add_intercept = TRUE}).
}
\description{
Calls the Julia function \strong{\code{Beta_estimate}} to obtain coefficient
estimates for OLS, GLM, multivariate-normal, or multinomial models.
When \code{family = "auto"} Julia chooses the estimator
automatically.
}
